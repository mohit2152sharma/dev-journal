[[dash]] in python is used to make interactive web apps.

It has two main concepts: layout and callbacks.
`layout`: defines how the app looks
`callbacks`: define the interactivity between the ui components and data